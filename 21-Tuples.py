#Tuples
#Strings Tuples are immutable whereas  lists are mutable

t = (1,2,3,4,5)
print(t)

#  Positive and negative indexing → like Lists
#  Slicing → like Lists
#  Iterating through a tuple → like Lists

# len(tuple)
# max(tuple)
# min(tuple)
#  tuple(seq)
#  sum(tuple)

print(len(t))
print(max(t))
print(min(t))
print(sum(t))

seq1 = "anurag"
seq2 = [1,2,3,4,'a','b']

print(tuple(seq1))
print(tuple(seq2))
